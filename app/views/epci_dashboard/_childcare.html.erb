<div class="bg-white shadow rounded-lg p-6 mb-6">
  <h2 class="text-lg font-medium text-gray-900 mb-4">Accueil de la petite enfance</h2>

  <!-- En-tête avec les statistiques globales -->
  <div class="bg-indigo-50 p-4 rounded-lg mb-6">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <div>
        <h3 class="text-sm font-medium text-indigo-800 mb-1">Taux de couverture moyen</h3>
        <p class="text-2xl font-bold text-indigo-900">
          <%= number_to_percentage(@epci_childcare_data&.dig("coverage_data", @epci_childcare_communes_data["year"].to_s, "coverage_rates", "global") || 0, precision: 1) %>
        </p>
        <p class="text-sm text-indigo-600">
          sur l'ensemble de l'EPCI
        </p>
      </div>
      <div>
        <h3 class="text-sm font-medium text-indigo-800 mb-1">Nombre de communes</h3>
        <p class="text-2xl font-bold text-indigo-900">
          <%= @epci_childcare_communes_data["communes_count"] %>
        </p>
      </div>
      <div>
        <h3 class="text-sm font-medium text-indigo-800 mb-1">Année de référence</h3>
        <p class="text-2xl font-bold text-indigo-900">
          <%= @epci_childcare_communes_data["year"] %>
        </p>
      </div>
    </div>
  </div>

  <!-- Comparaison avec les autres échelons territoriaux -->
  <div class="mb-6">
    <h3 class="text-sm font-medium text-gray-700 mb-2">Comparaison territoriale</h3>
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Échelon territorial
            </th>
            <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Taux de couverture global
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <!-- Ligne pour l'EPCI -->
          <tr>
            <td class="px-4 py-3 whitespace-nowrap text-sm font-medium text-gray-900">
              <%= display_territory_name(epci_display_name) %>
            </td>
            <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
              <div class="flex items-center">
                <% epci_rate = @epci_childcare_data&.dig("coverage_data", @epci_childcare_communes_data["year"].to_s, "coverage_rates", "global") %>
                <span class="<%= epci_rate && epci_rate > 0 ? "font-semibold" : "" %>">
                  <%= number_to_percentage(epci_rate, precision: 1) %>
                </span>
                <% if epci_rate && epci_rate > 0 %>
                  <div class="ml-2 w-16 bg-gray-200 rounded-full h-2">
                    <div class="bg-indigo-600 h-2 rounded-full" style="width: <%= [epci_rate * 100 / 100, 100].min %>%"></div>
                  </div>
                <% end %>
              </div>
            </td>
          </tr>

          <!-- Ligne pour le département -->
          <% if @department_childcare_data.present? %>
            <tr>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <%= department_display_name %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <div class="flex items-center">
                  <%
                    # Récupérer l'année correspondante
                    dept_year = @epci_childcare_communes_data["year"].to_s
                    dept_rate = @department_childcare_data&.dig("coverage_data", dept_year, "coverage_rates", "global")
                  %>
                  <span class="<%= dept_rate && dept_rate > 0 ? "font-semibold" : "" %>">
                    <%= number_to_percentage(dept_rate, precision: 1) %>
                  </span>
                  <% if dept_rate && dept_rate > 0 %>
                    <div class="ml-2 w-16 bg-gray-200 rounded-full h-2">
                      <div class="bg-blue-600 h-2 rounded-full" style="width: <%= [dept_rate * 100 / 100, 100].min %>%"></div>
                    </div>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>

          <!-- Ligne pour la région -->
          <% if @region_childcare_data.present? %>
            <tr>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <%= region_display_name %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <div class="flex items-center">
                  <%
                    # Récupérer l'année correspondante
                    region_year = @epci_childcare_communes_data["year"].to_s
                    region_rate = @region_childcare_data&.dig("coverage_data", region_year, "coverage_rates", "global")
                  %>
                  <span class="<%= region_rate && region_rate > 0 ? "font-semibold" : "" %>">
                    <%= number_to_percentage(region_rate, precision: 1) %>
                  </span>
                  <% if region_rate && region_rate > 0 %>
                    <div class="ml-2 w-16 bg-gray-200 rounded-full h-2">
                      <div class="bg-green-600 h-2 rounded-full" style="width: <%= [region_rate * 100 / 100, 100].min %>%"></div>
                    </div>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>

          <!-- Ligne pour la France -->
          <% if @france_childcare_data.present? %>
            <tr>
              <td class="px-4 py-3 whitespace-nowrap text-sm font-medium text-gray-900">
                France
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <div class="flex items-center">
                  <%
                    # Récupérer l'année correspondante
                    france_year = @epci_childcare_communes_data["year"].to_s
                    france_rate = @france_childcare_data&.dig("coverage_data", france_year, "coverage_rates", "global")
                  %>
                  <span class="<%= france_rate && france_rate > 0 ? "font-semibold" : "" %>">
                    <%= number_to_percentage(france_rate, precision: 1) %>
                  </span>
                  <% if france_rate && france_rate > 0 %>
                    <div class="ml-2 w-16 bg-gray-200 rounded-full h-2">
                      <div class="bg-red-600 h-2 rounded-full" style="width: <%= [france_rate * 100 / 100, 100].min %>%"></div>
                    </div>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="mt-2 text-xs text-gray-500">
      <p>Note: Les barres de progression sont calibrées pour un maximum de 100%.</p>
    </div>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- Carte des taux de couverture -->
    <div>
      <h4 class="text-sm font-medium text-gray-700 mb-2">Carte des taux de couverture par commune (<%= @epci_childcare_communes_data["year"] %>)</h4>
      <div id="communes-map-childcare" class="h-[500px] w-full border border-gray-200 rounded-md"></div>
      <div id="childcare-legend" class="mt-3"></div>
    </div>

    <!-- Tableau des communes avec les taux extrêmes -->
    <div>
      <h4 class="text-sm font-medium text-gray-700 mb-2">Communes aux taux extrêmes</h4>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-3 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Commune</th>
              <th class="px-3 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Taux de couverture</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <%
              # Communes avec les taux les plus élevés
              top_communes = @epci_childcare_communes_data["communes"]
                              .sort_by { |c| -c["global_coverage_rate"].to_f }
                              .first(3)

              # Communes avec les taux les plus bas
              bottom_communes = @epci_childcare_communes_data["communes"]
                              .sort_by { |c| c["global_coverage_rate"].to_f }
                              .first(3)
            %>

            <!-- Entête pour les communes avec les taux les plus élevés -->
            <tr>
              <td colspan="2" class="px-3 py-2 text-sm font-medium text-gray-900 bg-gray-100">Taux les plus élevés</td>
            </tr>

            <% top_communes.each do |commune| %>
              <tr>
                <td class="px-3 py-2 whitespace-nowrap text-sm text-gray-900"><%= commune["name"] %></td>
                <td class="px-3 py-2 whitespace-nowrap text-sm text-gray-900 font-medium text-green-600">
                  <%= number_to_percentage(commune["global_coverage_rate"], precision: 1) %>
                </td>
              </tr>
            <% end %>

            <!-- Entête pour les communes avec les taux les plus bas -->
            <tr>
              <td colspan="2" class="px-3 py-2 text-sm font-medium text-gray-900 bg-gray-100">Taux les plus bas</td>
            </tr>

            <% bottom_communes.each do |commune| %>
              <tr>
                <td class="px-3 py-2 whitespace-nowrap text-sm text-gray-900"><%= commune["name"] %></td>
                <td class="px-3 py-2 whitespace-nowrap text-sm text-gray-900 font-medium text-red-600">
                  <%= number_to_percentage(commune["global_coverage_rate"], precision: 1) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <!-- Boîte d'information explicative -->
  <div class="mt-6 bg-gray-50 p-4 rounded-md">
    <h4 class="text-sm font-medium text-gray-700 mb-2">À propos des taux de couverture</h4>
    <p class="text-sm text-gray-600">
      Le taux de couverture global représente la capacité théorique d'accueil pour 100 enfants de moins de 3 ans.
      Il inclut les places en accueil collectif (crèches), chez les assistantes maternelles et en préscolarisation.
      Un taux supérieur à 100% indique une capacité d'accueil supérieure au nombre d'enfants résidant dans la commune.
    </p>
  </div>
</div>

<!-- Bloc contextuel : Taux de couverture global -->
<div class="mt-6 bg-gradient-to-r from-violet-50 to-purple-50 border-l-4 border-violet-500 rounded-lg p-6 shadow-sm">
  <div class="flex items-start space-x-4">
    <!-- Icône -->
    <div class="flex-shrink-0">
      <div class="p-3 bg-violet-100 rounded-lg">
        <svg class="w-6 h-6 text-violet-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
        </svg>
      </div>
    </div>

    <!-- Contenu -->
    <div class="flex-1">
      <h4 class="text-lg font-semibold text-gray-900 mb-3">
        🏛️ Lecture du tableau – Taux de couverture global (TCG) de l'accueil du jeune enfant
      </h4>

      <div class="space-y-3 text-sm text-gray-700 leading-relaxed">
        <p class="font-medium text-gray-800">
          <span class="bg-violet-100 px-2 py-1 rounded">Comprendre le taux de couverture global</span>
        </p>

        <p>
          Le taux de couverture global rapporte l'<strong>ensemble des places d'accueil formel</strong>
          (crèches, assistantes maternelles, garde à domicile, etc.) au nombre d'enfants de moins de 3 ans sur un territoire.
          Il constitue un <strong>indicateur clé du pilotage</strong> de la politique petite enfance, permettant d'évaluer
          la capacité du territoire à répondre aux besoins des familles.
        </p>

        <div class="bg-white border border-violet-200 rounded-lg p-4">
          <p class="font-medium text-gray-900 mb-2">📊 Interprétation du TCG</p>
          <div class="space-y-2 text-gray-700">
            <div class="flex items-start space-x-2">
              <span class="text-red-600 font-bold mt-0.5">🔴</span>
              <p>
                <strong>TCG faible</strong> traduit un risque de <span class="font-semibold text-violet-700">sous-capacité d'accueil</span>
                et donc de non-satisfaction de la demande
              </p>
            </div>
            <div class="flex items-start space-x-2">
              <span class="text-green-600 font-bold mt-0.5">🟢</span>
              <p>
                <strong>TCG élevé</strong> reflète une offre plus abondante, mais qui peut masquer d'importantes
                <span class="font-semibold text-violet-700">disparités locales</span> entre communes ou quartiers
              </p>
            </div>
          </div>
        </div>

        <div class="bg-purple-50 border border-purple-300 rounded-lg p-4">
          <p class="font-medium text-gray-900 mb-2">🗺️ Enjeux d'équité territoriale et sociale</p>
          <p class="text-gray-700 mb-2">
            L'analyse de ce taux éclaire les enjeux d'équité territoriale et sociale :
          </p>
          <ul class="list-disc list-inside space-y-1 text-gray-700 ml-2">
            <li>Les <strong>zones urbaines aisées</strong> disposent souvent d'une offre collective dense</li>
            <li>Les <strong>territoires ruraux ou populaires</strong> s'appuient surtout sur les assistantes maternelles</li>
          </ul>
        </div>

        <div class="bg-white border border-violet-200 rounded-lg p-4">
          <p class="font-medium text-gray-900 mb-2">🎯 Pilotage pour les EPCI</p>
          <p class="text-gray-700">
            Pour les EPCI, suivre l'évolution du TCG permet de :
          </p>
          <ul class="list-disc list-inside space-y-1 text-gray-700 mt-2 ml-2">
            <li><strong>Prioriser les investissements</strong> en fonction des besoins identifiés</li>
            <li><strong>Diversifier les modes d'accueil</strong> pour répondre à tous les profils de familles</li>
            <li>
              <strong>Coordonner les financements</strong> CAF–État–collectivités dans le cadre du
              Service public de la petite enfance (SPPE)
            </li>
          </ul>
        </div>

        <p class="italic text-gray-600 border-l-2 border-violet-300 pl-3 mt-4">
          L'objectif est de tendre vers une <strong>couverture universelle et équitable</strong> de l'accueil
          des moins de 3 ans, garantissant à chaque famille un accès effectif aux modes de garde adaptés à ses besoins.
        </p>

        <div class="mt-4 pt-3 border-t border-gray-200">
          <p class="text-xs text-gray-500">
            <strong>Sources :</strong> CAF – DREES (portails de données territoriales, 2024) •
            Cour des comptes, <em>La politique d'accueil du jeune enfant</em>, 2024 •
            Ministère des Solidarités, <em>Service public de la petite enfance</em>, 2023
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 🆕 PROJECTION TAUX DE COUVERTURE 2035 -->
<% if @childcare_coverage_projection.present? %>
<div class="mt-8 bg-gradient-to-r from-cyan-50 to-blue-50 border-l-4 border-cyan-500 rounded-lg p-6 shadow-sm mb-6">
  <div class="flex items-start space-x-4">
    <!-- Icône -->
    <div class="flex-shrink-0">
      <div class="p-3 bg-cyan-100 rounded-lg">
        <svg class="w-6 h-6 text-cyan-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
        </svg>
      </div>
    </div>

    <!-- Contenu -->
    <div class="flex-1">
      <h4 class="text-lg font-semibold text-gray-900 mb-4">
        🔮 Projection du taux de couverture en 2035
      </h4>

      <!-- KPI actuels -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
        <div class="bg-white rounded-lg p-4 border border-cyan-200">
          <p class="text-sm text-gray-600 font-medium mb-1">Enfants 0-3 ans (année actuelle)</p>
          <p class="text-3xl font-bold text-cyan-700">
            <%= number_with_delimiter(@childcare_coverage_projection[:current_children], delimiter: " ") %>
          </p>
        </div>

        <div class="bg-white rounded-lg p-4 border border-cyan-200">
          <p class="text-sm text-gray-600 font-medium mb-1">Enfants 0-3 ans (projection 2035)</p>
          <p class="text-3xl font-bold text-cyan-700">
            <%= number_with_delimiter(@childcare_coverage_projection[:projected_children], delimiter: " ") %>
          </p>
          <p class="text-xs text-gray-500 mt-1">
            <span class="<%= @childcare_coverage_projection[:children_evolution_pct] >= 0 ? 'text-green-600' : 'text-red-600' %>">
              <%= @childcare_coverage_projection[:children_evolution_pct] >= 0 ? '+' : '' %><%= @childcare_coverage_projection[:children_evolution_pct] %>%
            </span>
          </p>
        </div>

        <div class="bg-white rounded-lg p-4 border border-cyan-200">
          <p class="text-sm text-gray-600 font-medium mb-1">Places d'accueil actuelles</p>
          <p class="text-3xl font-bold text-cyan-700">
            <%= number_with_delimiter(@childcare_coverage_projection[:current_places], delimiter: " ") %>
          </p>
          <p class="text-xs text-gray-500 mt-1">
            (Taux <%= number_to_percentage(@childcare_coverage_projection[:current_rate], precision: 1) %>)
          </p>
        </div>
      </div>

      <!-- 3 Scénarios -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
        <% @childcare_coverage_projection[:scenarios].each do |scenario_key, scenario| %>
          <div class="bg-white border-2 <%= scenario_key == :proportional ? 'border-cyan-500 shadow-md' : 'border-gray-200' %> rounded-lg p-4">
            <p class="font-semibold text-gray-900 mb-3">
              <%= scenario[:label] %>
              <% if scenario_key == :proportional %>
                <span class="ml-2 inline-block px-2 py-1 bg-cyan-100 text-cyan-800 text-xs rounded font-medium">Référence</span>
              <% end %>
            </p>

            <div class="space-y-2 mb-4">
              <div>
                <p class="text-sm text-gray-600">Taux de couverture projeté</p>
                <p class="text-2xl font-bold <%= scenario[:evolution] >= 0 ? 'text-green-600' : 'text-red-600' %>">
                  <%= number_to_percentage(scenario[:rate], precision: 1) %>
                </p>
              </div>

              <div class="flex justify-between items-center pt-2 border-t border-gray-200">
                <span class="text-xs text-gray-500">Évolution</span>
                <span class="text-sm font-semibold <%= scenario[:evolution] >= 0 ? 'text-green-600' : 'text-red-600' %>">
                  <%= scenario[:evolution] >= 0 ? '+' : '' %><%= scenario[:evolution] %>%
                </span>
              </div>

              <div class="flex justify-between items-center">
                <span class="text-xs text-gray-500">Places nécessaires</span>
                <span class="text-sm font-medium text-gray-900">
                  <%= number_with_delimiter(scenario[:places], delimiter: " ") %>
                </span>
              </div>
            </div>

            <p class="text-xs text-gray-600 italic">
              <%= scenario[:description] %>
            </p>
          </div>
        <% end %>
      </div>

      <!-- Bloc méthodologique -->
      <div class="bg-white border border-cyan-200 rounded-lg p-4">
        <p class="font-semibold text-gray-900 text-sm mb-3">📌 Méthodologie</p>
        <div class="space-y-2 text-sm text-gray-700">
          <p>
            <strong>Formule :</strong> Taux 2035 = (Places 2035 / Enfants projetés 2035) × 100
          </p>
          <p>
            <strong>Places actuelles :</strong> <%= number_with_delimiter(@childcare_coverage_projection[:current_places], delimiter: " ") %>
            = <%= number_to_percentage(@childcare_coverage_projection[:current_rate], precision: 1) %> ×
            <%= number_with_delimiter(@childcare_coverage_projection[:current_children], delimiter: " ") %> enfants
          </p>
          <p class="bg-cyan-50 p-2 rounded border border-cyan-200">
            Les trois scénarios testent différentes hypothèses d'offre : une offre stable, une offre qui suit la démographie,
            ou une offre volontariste (+20%). Le scénario de référence (proportionnel) suppose un maintien de la couverture
            malgré l'évolution du nombre d'enfants.
          </p>
        </div>
      </div>

      <!-- Alerte contextuelle -->
      <div class="mt-4 bg-amber-50 border border-amber-300 rounded-lg p-4">
        <p class="font-medium text-amber-900 text-sm mb-2">⚠️ Contexte crucial</p>
        <p class="text-sm text-amber-800">
          Cette projection dépend fortement de la dynamique future de l'offre d'accueil.
          Elle suppose une répartition stable des places par mode d'accueil.
          Le vieillissement des assistantes maternelles pourrait réduire l'offre individuelle indépendamment de ces scénarios.
        </p>
      </div>
    </div>
  </div>
</div>
<% end %>

<!-- Données JSON pour la carte -->
<script type="application/json" id="communes-childcare-geojson">
  <%= raw(@communes_childcare_geojson) %>
</script>
